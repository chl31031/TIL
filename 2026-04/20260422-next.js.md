# ⚛️ Next.js 입문 및 커스텀 훅 (Custom Hooks)

## 1. Custom Hook (커스텀 훅)

반복되는 상태 관리 로직을 별도의 함수로 추출하여 재사용하는 패턴입니다.

### 핵심 규칙 및 특징

- **이름 컨벤션**: 반드시 `use`로 시작해야 합니다 (예: `useFetch`).
- **조합**: 내부에서 `useState`, `useEffect` 등 기존 Hook을 자유롭게 조합하여 새로운 기능을 만듭니다.

### 실습 예시: `useFetch`

API 통신 시 발생하는 로딩, 에러, 데이터 상태를 관리하는 커스텀 훅입니다.

```javascript
function useFetch(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const controller = new AbortController(); // fetch 요청 중단용

    fetch(url, { signal: controller.signal })
      .then((response) => {
        if (!response.ok) throw new Error("네트워크 응답 문제가 있습니다.");
        return response.json();
      })
      .then((responseData) => setData(responseData))
      .catch((e) => {
        if (e.name === "AbortError") {
          console.log("통신이 정상적으로 중단되었습니다.");
        } else {
          setError(e);
        }
      })
      .finally(() => setLoading(false));

    return () => controller.abort(); // 클린업: 언마운트 시 요청 취소
  }, [url]);

  return { data, loading, error };
}
```

---

## 2. Next.js 소개 및 주요 특징

Next.js는 React 기반의 풀스택 프레임워크로, 강력한 성능과 개발 편의성을 제공합니다.

- **서버 사이드 렌더링 (SSR)**: 서버에서 HTML을 미리 완성하여 전송.
- **파일 기반 라우팅**: `app/` 폴더 구조가 곧 URL 경로가 됨.
- **풀스택 환경**: API Route를 통해 백엔드 코드 작성이 가능.

### 프로젝트 구조 (`src/app/`)

- `layout.js`: 공통 레이아웃 (루트 및 중첩 레이아웃).
- `page.js`: 실제 해당 경로의 페이지 내용.
- `[id]/page.js`: 동적 라우트 세그먼트.

---

## 3. Next.js 라우팅 및 레이아웃

### ① Link 컴포넌트

`<a>` 태그 대신 `<Link href="/">`를 사용하여 새로고침 없는 클라이언트 사이드 전환을 지원합니다.

### ② 클라이언트 Hook (Next.navigation)

`"use client"` 지시어가 선언된 컴포넌트에서만 사용 가능합니다.
| Hook | 역할 |
| :--- | :--- |
| `usePathname()` | 현재 URL 경로 문자열 반환 |
| `useParams()` | 동적 라우트의 파라미터 값 반환 (`[id]`) |
| `useSearchParams()` | 쿼리스트링 파라미터 읽기 (`?name=abc`) |
| `useRouter()` | 프로그래밍 방식 페이지 이동 (`push`, `replace`) |

### ③ 중첩 레이아웃 (Nested Layout)

특정 폴더(`app/dashboard/`) 안에 `layout.js`를 배치하면 해당 경로와 하위 페이지에만 적용되는 전용 레이아웃을 구성할 수 있습니다.

---

## 4. 동적 라우팅 및 파라미터 제어

### 동적 세그먼트 활용

- 폴더명을 `[menuCode]`와 같이 대괄호로 감싸면 해당 위치의 URL 값이 변수가 됩니다.
- **데이터 추출 예시**:

```javascript
"use client";
import { useParams, useSearchParams } from "next/navigation";

export default function Detail() {
  const { menuCode } = useParams(); // /menu/123 -> 123
  const searchParams = useSearchParams();
  const name = searchParams.get("name"); // ?name=coffee -> coffee

  return <div>메뉴 코드: {menuCode}</div>;
}
```

---

## 5. Next.js App Router 핵심 규칙 요약

| 항목                    | 규칙 및 내용                                         |
| :---------------------- | :--------------------------------------------------- |
| **페이지 정의**         | `app/폴더/page.js`                                   |
| **공통 UI**             | `app/폴더/layout.js`                                 |
| **클라이언트 컴포넌트** | Hook이나 이벤트 사용 시 맨 위에 `"use client"` 필수  |
| **경로 별칭**           | `@/`를 통해 `src/` 디렉토리 기준 절대 경로 접근 가능 |
