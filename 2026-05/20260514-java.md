**오늘의 한 줄 평:**

#### 꿀팁

- 2026-05-14 수업 27일차
- Data : 관찰의 결과로 나타난 정량적 혹은 정성적인 실제 값
- Database : 한 조직에 필요한 정보를 여러 응용 시스템에서 공유할 수 있도록 논리적으로 연관된 데이터를 모으고, 중복되는 데이터를 최소화하여 구조적으로 통합/저장

#### DBMS

- 데이터 베이스 관리 시스템 (관리인)

#### MY SQL

- RDMBS : 관계형 데이터베이스 관리 시스템
- 오라클사 소유. 가장 널리 사용되는 오픈 소스 관계형 데이터베이스 관리 시스템
- 오픈 소스. 소스 코드를 누구나 자유롭게 기여해서 발전해나갈 수 있다.

#### MY SQL 동작 원리

- 클라이언트/서버 통신 > 쿼리 파싱 > 최적화 및 실행 계획 생성 > 쿼리 실행 > 결과 반환
- 설치방법 PDF 파일 참고

#### MySQL 개발 환경 설정 (Window)

- - MS SQL 전체 삭제 : C > programfiles, programdata > MYSQL 폴더 삭제
- mysql 실행중인 경우 강제 종료
  - taskkill /f /im mysqld.exe

#### MYSQL

- 줄 커서 + Ctrl + Enter > 해당 줄 명령 실행

```
-- 계정 생성 후 데이터베이스 활용

-- 1) 새로운 계정 만들기
CREATE USER 'ohgiraffers'@'%' IDENTIFIED BY 'ohgiraffers';

-- 현재 존재하는 데이터베이스 확인
SHOW databases;

-- mysql 데이터베이스로 계정 정보 확인하기
USE mysql;

SELECT * FROM USER;

-- 2) 데이터베이스 생성 후 계정에 권한 부여
-- 데이터베이스(스키마)생성
CREATE DATABASE menudb;

GRANT ALL PRIVILEGES ON menudb.* TO 'ohgiraffers'@'%';

SHOW GRANTS FOR 'ohgiraffers'@'%';

USE menudb;
```

#### connection 추가

메인에서 + 클릭 > connection 추가. //ohgiraffers 생성
이전 명령어로 생성한 menudb 생성됨 확인
use menudb 혹은 왼쪽 목록에서 DB 더블클릭> 굵은 글씨

아래 코드 전체 실행
tbl_categroy, tbl_menu에 데이터 추가 확인

+sql > 새 sql문 페이지 열림

#### sql문 실습

- 하나의 구문 끝날 때 ;
- SELECT 소문자로 써도 되지만 대문자 권장

### JOIN

#### SELF JOIN

Self Join(셀프 조인)은 하나의 테이블이 두 가지 역할을 동시에 수행한다고 생각하면 이해가 빠릅니다.

지금 작성하신 코드를 예로 들어, 왜 이 구조가 필요한지 핵심만 짚어드릴게요.

1. 왜 테이블을 두 번 쓰나요? (핵심 개념)
   데이터는 하나인데, 그 안에 '계급(상하 관계)'이 섞여 있기 때문입니다.

a (하위 역할): "나는 누구의 밑에 있는가?"를 찾는 테이블

b (상위 역할): "내가 누구의 상사(부모)인가?"를 보여주는 테이블

2. JOIN 조건 해석하기
   ON a.ref_category_code = b.category_code

a.ref_category_code: 하위 카테고리가 들고 있는 "우리 부모님 번호"입니다.

b.category_code: 상위 카테고리가 들고 있는 "내 고유 번호"입니다.

쉽게 말해:
"내(a)가 들고 있는 '부모 번호'와 일치하는 '고유 번호'를 가진 데이터(b)를 옆에 붙여줘!"라는 뜻입니다.

#### SQL 실행 순서

SELECT - 어떤 테이블에서부터
FROM - 어떤 조건의 필터에 맞춰서
WHERE - 어떤 그룹으로 묶은 다음에
GROUP BY - 어떤 순서로 묶을지
HAVING - 어떤 조건을 가져올지
ORDER BY - 어떤 순서로 보여줄지

### 실습 정리

1. SELECT

```
# select
-- SELECT 컬럼명 FROM 테이블명;
-- 특정 데이터에서 원하는 데이터를 조회하는데 사용

-- 단일 열(컬럼) 데이터 검색
SELECT menu_name FROM tbl_menu;

-- 여러 열의 데이터 검색
SELECT
	menu_code,
    menu_name,
    menu_price
FROM
	tbl_menu;

-- 모든 열에서 데이터 검색( '*' : 모든 컬럼을 의미하는 와일드 카드)
SELECT * FROM tbl_menu;

-- FROM 절 없이 간단한 연산
SELECT 6 + 3;
SELECT 6 * 3;

-- 내장 함수 사용
SELECT NOW(); -- 현재 날짜와 시간을 보여줌
SELECT CONCAT('홍', '길동'); -- 여러 문자열을 합쳐준다.

-- 컬럼 별칭(Alias) 사용
SELECT CONCAT('홍', '길동') AS name ; -- 별칭
SELECT CONCAT('홍', '길동') AS 'Full name'; -- 별칭 띄워쓰기 시 '' 사용
```

2. ORDER BY

```
# ORDER BY 기준컬럼
-- SELECT 문의 가장 마지막에 위치하며, 결과 집합을 하나의 열로 정렬

-- 오름차순 정렬
SELECT
	menu_code,
    menu_name,
    menu_price
FROM
	tbl_menu
ORDER BY
	menu_price ASC; -- ASC는 오름차순을 의미(생략 가능)

-- 내림차순 정렬
SELECT
	menu_code,
    menu_name,
    menu_price
FROM
	tbl_menu
ORDER BY
	menu_price DESC, -- 내림차순 (DESC 생략 불가능)
    menu_name ASC;   -- 2차 기준 : 가격이 같다면, 이름 오름차순으로

-- 연산 결과로 정렬(보통 별칭을 사용)
SELECT
	menu_code,
    menu_price,
    menu_code * menu_price AS calculated_value -- 곱한 결과를 select절 조회
FROM
	tbl_menu
ORDER BY
	menu_code * menu_price DESC;

-- FIELD(컬럼, 첫번째, 두번째, ...) : 컬럼 값이 목록의 몇 번째에 있는지 숫자로 알려준다.
SELECT FIELD('B', 'A', 'B', 'C');

-- 'N'을 1순위 'Y'를 2순위로 정렬
SELECT
	menu_name,
    orderable_status
FROM
	tbl_menu
ORDER BY
	FIELD(orderable_status, 'N', 'Y');

-- null 값이 있는 컬럼에 대한 정렬
SELECT
	category_code,
    category_name,
    ref_category_code
FROM
	tbl_category -- 카테고리 테이블에서 부터
ORDER BY
 -- ref_category_code IS NULL; -- NULL이면 TRUE(1), 아니면 FALSE(0) > NULL이 가장 아래 위치
 ref_category_code IS NULL DESC, -- NULL이 상단에 오도록 정렬
 ref_category_code DESC;
```

3. WHERE

```
# WHERE 조건
-- '조건 필더링'의 역할, 특정 조건에 맞는 레코드만을 선택하는데 사용
-- 1. 비교 연산자 활용(=, <>, >, <=)

SELECT
	menu_name,
    menu_price,
    orderable_status
FROM
	tbl_menu
WHERE
	orderable_status <> 'Y'; -- 해당 테이블과 값이 같지 않은 데이터 TRUE
SELECT
	menu_name,
    menu_price
FROM
	tbl_menu
WHERE
	menu_price < 13000;

-- 2. 논리 연산자(AND, OR)를 활용하여 조건 조합
SELECT
	menu_name,
    category_code,
    orderable_status
FROM
	tbl_menu
WHERE
	orderable_status = 'Y' AND category_code = 10;

-- OR(둘 중 하나라도 TRUE)
-- 가격이 만원 이상이고, 2만 5천원인 메뉴 찾기
SELECT
	menu_name,
    menu_price
FROM
	tbl_menu
WHERE
	menu_price >= 10000 AND menu_price <= 25000;

-- BETWEEN 연산자
SELECT
	menu_name,
    menu_price
FROM
	tbl_menu
WHERE
	-- 10000 이상 25000 이하(부정은 NOT을 붙인다)
	menu_price BETWEEN 10000 AND 250000;

-- LIKE
-- 메뉴 이름에 '마늘'이 포함된 메뉴 찾고 싶을 때, %는 0개 이상의 모든 문자를 의미하는 와일드 카드
SELECT
	menu_name
FROM
	tbl_menu
WHERE
	menu_name LIKE '%마늘%' ;
    -- LIKE '%마늘' -- '마늘'로 끝나는 모든 것 (햇마늘, 깐마늘, 의성마늘 등)
    -- LIKE '마늘%' -- 중간에 '마늘'이 들어가는 모든 것

-- IN 연산자 : OR 연산자를 여러 번 쓰는 대신, 괄호 ( ) 안에 찾고 싶은 값들을 쉼표로 나열해서 사용
-- 카테고리 코드가 4번 OR 5번 OR 6번인 메뉴 찾기
SELECT
	menu_name,
    category_code
FROM
	tbl_menu
WHERE
	category_code IN (4,5,6);

-- IS NULL 연산자
-- 데이터베이스에서 NULL은 '0'이나 '빈 문자열'이 아닌, '값이 존재하지 않음'을 의미하는 상태이다.
-- 상위 카테고리가 없는 (NULL) 카테고리 찾기
SELECT
	category_code,
    category_name,
    ref_category_code
FROM
	tbl_category
WHERE
	ref_category_code IS NULL; -- *부정 : IS NOT NULL

```

4. DISTINT

```
# DISTINCT
-- 중복 값을 제거하고 유니크한 값들의 목록만 출력
-- 특정 컬럼에 어떤 값들이 '종류별로'있는지 확인할 때 유용

SELECT DISTINCT
	category_code
FROM
	tbl_menu
ORDER BY
	category_code;

-- NULL 값을 포함한 열
SELECT distinct
	ref_category_code
FROM
	tbl_category;

-- 열이 여러개인 distinct
-- 지정된 모든 열의 조합이 똑같을 때만 중복으로 간주하여 제거
SELECT distinct
	category_code
    orderable_status
FROM
	tbl_menu;
```

5. LIMIT

```
# LIMIT
-- 결과중에 딱 내가 원하는 마늠만 잘라서 줘! 라고 요청하는 키워드

-- LIMIT [개수]
SELECT
	menu_name,
    menu_price
FROM
	tbl_menu
ORDER BY
	menu_price DESC
LIMIT 5;

-- LIMIT [시작 위치(건너뛸 개수)]
-- 2번째 행부터 4개의 행을 가져오기
SELECT
	menu_code,
    menu_name,
    menu_price
FROM
	tbl_menu
ORDER BY
	menu_price DESC
LIMIT 1, 4;
```

6. JOIN

```
# JOIN

-- 1. 컬럼 별칭
SELECT
	menu_name AS '메뉴 이름',
    menu_price
FROM
	tbl_menu;

-- 2. 테이블 별칭 (JOIN에서 필수)
SELECT
	menu_name,
    menu_price
FROM
	tbl_menu AS a;

-- INNER JOIN(교집합) - 가장 기본적인 조인, INNER 키워드 생략 가능
SELECT
	a.menu_name,
    b.category_name
FROM
	tbl_menu a
JOIN
	tbl_category b ON a.category_code = b.category_code;

SELECT *
FROM tbl_category;

-- OUTER JOIN (LEFT / RIGHT)

-- LEFT : 조인 구문을 기준으로 '왼쪽' 테이블의 데이터는 모두 보여주고,
-- 오른쪽 테이블에서 짝이 맞는 데이터가 없으면 'NULL'로 채워 보여준다.
-- RIGHT : 기준이 오른쪽 테이블이다.
SELECT
	a.category_name,
    b.menu_name
FROM
	tbl_category a
LEFT JOIN tbl_menu b ON a.category_code = b.category_code;

-- SELF JOIN
SELECT
	a.category_name AS '하위 카테고리',
    b.category_name AS '상위 카테고리'
FROM
	tbl_category a -- 하위 역할
JOIN tbl_category b ON a.ref_category_code = b.category_code; -- 상위 역할

```

### 과제 어려운 부분 공부

```
SELECT
e.EMP_NAME AS '직원명',
d.DEPT_TITLE AS '부서명',
m.EMP_NAME AS '관리자명',
md.DEPT_TITLE AS '관리자 부서명'
FROM employee e
-- 1단계 : 직원의 부서 정보 가져오기
JOIN department d ON e.DEPT_CODE = d.DEPT_ID
-- 2단계 :관리자가 존재하는 직원만 출력
JOIN employee m ON e.MANAGER_ID = m.EMP_ID
-- 3단계 : 관리자의 부서 정보를 가져오기 위해 연결
JOIN department md ON m.DEPT_CODE = md.DEPT_ID
ORDER BY e.EMP_NAME ASC;
```

```
1. 테이블 별칭(Alias)의 활용
쿼리를 보면 employee e, employee m, department d 처럼 테이블 뒤에 알파벳이 붙어 있습니다. 이는 별칭으로, 같은 테이블이라도 서로 다른 역할을 부여하기 위해 사용합니다.

e: Employee (대상 직원 정보)

m: Manager (직원의 관리자 정보 - 사실 똑같은 직원 테이블입니다)

d: Department (직원의 부서 이름)

md: Manager's Department (관리자의 부서 이름)

🛠 단계별 조인(JOIN) 분석
1단계: 직원의 부서 정보 연결
SQL
JOIN department d ON e.DEPT_CODE = d.DEPT_ID
내용: 직원 테이블(e)에 있는 부서 코드와 부서 테이블(d)의 ID를 매칭합니다.

결과: 직원이 속한 부서의 이름(DEPT_TITLE)을 가져올 수 있게 됩니다.

2단계: 관리자 정보 연결 (Self Join)
SQL
JOIN employee m ON e.MANAGER_ID = m.EMP_ID
내용: 직원의 '관리자 사번'(MANAGER_ID)을 가지고 다시 직원 테이블(m)에서 해당 사번을 가진 사람을 찾습니다. 이를 셀프 조인(Self Join)이라고 합니다.

특이사항: INNER JOIN 방식이므로, 관리자가 없는 직원(사장님 등)은 결과에서 제외됩니다.

3단계: 관리자의 부서 정보 연결
SQL
JOIN department md ON m.DEPT_CODE = md.DEPT_ID
내용: 위에서 찾은 관리자(m)가 어느 부서 소속인지 알기 위해, 관리자의 부서 코드와 부서 테이블(md)을 한 번 더 연결합니다.

결과: '관리자의 부서명'까지 완벽하게 출력할 수 있습니다.
```
