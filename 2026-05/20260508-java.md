**오늘의 한 줄 평:**

#### 꿀팁

- toString : alt+insert로 타자 치지 않고 빠르게 입력 가능
- Alt + Enter : 클래스 자동 입력
- soutv : 값을 출력
  2026-05-08 수업 23일차

### API: Application Programming Interface

- 자바에서 자주 사용되는 클래스 및 인터페이스의 모음
- alt + insert > 오버라이드 > java.lang.Object 표시되는 이유는 내가 Object를 따로 상속하지 않아도 여태까지 상속해서 쓰고 있었다는 뜻

#### IDE로 equals 오버라이딩 자동화

- Object Class의 equals > 오로지 주소값(==)만 비교

```
public boolean equals(Object obj) {
    return (this == obj);
}
```

- IDE로 equals 오버라이딩 자동화
  - eq 적고 드래그 > equals() 및 hashcode() 생성 > 모두 선택 > equals 오버라이딩 메소드 생성
  - 혹은 상단 코드 > 생성 > equals() 및 hashcode() 클릭 > 비교 재정의 기준을 정할 필드를 선택 > 자동 오버라이딩 완료
  - 생성된 코드

```
    @Override
    public boolean equals(Object o) {
        if (o == null || getClass() != o.getClass()) return false;
        Book book = (Book) o;
        return number == book.number && price == book.price && Objects.equals(title, book.title) && Objects.equals(author, book.author);
    }

    @Override
    public int hashCode() {
        return Objects.hash(number, title, author, price);
    }
```

- boo2.equals의 값이 오버라이딩 후 false > true로 바뀐다.
  - 기존에는 주소값 만 비교(==)하다, 필드의 내용이 같은지 비교 하게 오버라이딩 했기 때문

```
   Book book1 = new Book(1, "홍길동전", "허균", 500000);
   Book book2 = new Book(2, "목민심서", "정약용", 300000);
   Book book3 = new Book(2, "목민심서", "정약용", 300000);

   System.out.println("동일성 비교(==)" + (book2 == book3));
   System.out.println("동등성 비교"+ book2.equals(book3)); //동등성 비교를 위해 Override 한 후 결과 true로 바뀜
```

- 참고 : String 클래스의 equals
- - instanceof String aString이나 COMPACT_STRINGS 같은 표현. "상대방이 문자열(String)인지 확인하고, 문자 하나하나가 일치하는지 검사하자"라는 String 클래스만의 특별한 규칙

```public boolean equals(Object anObject) {
        if (this == anObject) {
            return true;
        }
        return (anObject instanceof String aString)
                && (!COMPACT_STRINGS || this.coder == aString.coder)
                && StringLatin1.equals(value, aString.value);
    }

```

#### hashCode 오버라이드

- hashCode 오버라이드 전에는 주소값이 다르게 나오지만,
- 오버라이드 후에는 필드값이 같으면 생성된 해쉬코드도 동일
- 생성된 메서드

```
    @Override
    public int hashCode() {
        return Objects.hash(number, title, author, price);
    }
```

- 동일한 주소

```
        System.out.println(book2.hashCode());
        System.out.println(book3.hashCode());
```

- HashSet을 생성 > Boo2를 넣음 > Book3이 포함 되었는지 확인
  - equals를 오버라이딩 시 결과값 true

```
Set<Book> books = new HashSet<>();
        books.add(book2);
        System.out.println("HashSet에서 book3 찾기 : " + books.contains(book3));
```

#### String 클래스

- trim() : 앞 뒤 공백 제거. (사이 공백 제거하지 않음)
- toUpperCase(): 대문자 변환(검색/비교 시 대소문자 통일)
- substring(start, end) : 특정 구간 잘라내기
- replace(): 특정 문자열 치환
- '==' vs equals

```
        String str1 = "java";
        String str2 = "java";
        String str3 = new String("java");
        String str4 = new String("java");
```

- 결과
- str1 == str2 는 true
- str3 == str4 는 false
- 왜 str1 == str2 는 true 일까?

```
자바에서 쌍따옴표("")를 이용해 문자열을 만들면, 자바는 메모리의 힙(Heap) 영역 안에 있는 문자열 상수 풀이라는 특별한 저장소를 먼저 확인합니다.
str1 = "java" 실행 시: "상수 풀에 'java'라는 글자가 있나?" 확인합니다. 없으니까 새로 만들고 그 주소를 str1에 줍니다.
str2 = "java" 실행 시: "상수 풀에 'java'가 있나?" 확인합니다. 방금 str1이 만든 게 이미 있죠?
결과: 자바는 메모리를 아끼기 위해 새로 만들지 않고, 이미 있는 'java'의 주소값을 str2에게도 똑같이 줍니다.
결국 str1과 str2는 똑같은 메모리 주소를 가리키게 되므로, 주소를 비교하는 == 결과가 true가 나오는 것입니다.
```

- split(): 문자열 분리하기

```
        String emp = "100/홍길동/서울/영업부";
        String[] empArr = emp.split("/"); //분리한 문자열을 배열에 저장
        for(String info : empArr){
            System.out.println(info.isEmpty() ? "정보 없음" : info);
        }
```

- 이스케이프 문자

```
\n : 개행
\t : 탭
\" : "(쌍따옴표) 표시
```

#### StringBuilder

- StringBuilder : String 연산을 효율적으로 할 수 있다.
- delete(시작*인덱스, 끝*인덱스) : 특정 부분 삭제 (시작 인덱스 ~ 끝 인덱스-1 위치 삭제)
- insert(인덱스, 값) : 원하는 위치에 문자열 삽입
- reverse() : 문자열의 순서를 뒤집는다.

### wrapper 클래스

- 기본 타입의 데이터를 객체화 할 때 사용

```
byte	Byte
char	Character
int	Integer
float	Float
double	Double
boolean	Boolean
long	Long
short	Short
```

### time 클래스

- LocalDate.of
- LocalTime.of
- LocalDateTime.of
- getYear(), getMonth(), getMonthValue(), getDayOfWeek(), getHour()
- minusMinutes
  - LocalDateTime trainDeparture = LocalDateTime.of(departureDate, departureTime);
  - trainDeparture.minusMinutes(20)
- .isBefore(now)
- DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy년 MM월 dd일 HH시 mm분 출발");
- DateTimeFormatter christmasParser = DateTimeFormatter.ofPattern("yyMMdd");
  - 특정 형식의 문자열을 날짜 객체로 변환(분석, Parse)

### Generic

- 타입이 확정되지 않은 설계도
- <T> : 타입 파라미터(Type Parameter). T는 타입을 담는 변수 처리.
- 이 클래스를 사용할 때 T 자리에 실제 타입을 넣는다.

```
public class GenericBox<T> {

    private T content; //변수명이 'T'일 필요는 없지만, 보통 그렇게 씀.

    public T getContent() {
        return content;
    }

    public void setContent(T content) {
        this.content = content;
    }
}
```

```
public class Application {
    public static void main(String[] args) {

        NormalBox normalBox = new NormalBox();
        normalBox.setContent("안녕하세요");

        //꺼낼 떄 반드시 강제 형변환
        //Object로 꺼내온 자료형을 String에 다운캐스팅 없이 담으면 에러
        //String content = normalBox.getContent();
        String content = (String)normalBox.getContent();

        normalBox.setContent(123); //실수로 숫자를 넣어도 컴파일 에러 안남

        //String wrongContent = (String) normalBox.getContent();
        GenericBox<String> stringBox = new GenericBox<>();

        stringBox.setContent("hello world");
        String strContent = stringBox.getContent();
        System.out.println("strContent = " + strContent);

        //제네릭에 들어가는 타입은 반드시 '객체 타입(참조형)'이어야 한다.
        //내부적으로 타입을 Object로 다루기 때문에, 기본형은 object로 업캐스팅 할 수 없다.
        GenericBox<Integer> integerBox = new GenericBox<>();
        integerBox.setContent(100);

        int intContent = integerBox.getContent(); //auto-unboxing으로 int 변수에 담기 가능

    }
}
```

- 제한된 타입 파라미터(Bounded Type Parameter)
  - RabbitFarm<T extends Rabbit>
- 제네릭 와일드 카드
  - RabbitFarm의 어떤 타입이든 받겠다.

```
    public void managerAnyFarm(RabbitFarm<?> farm){
        System.out.println("어떤 토끼 농장이든 관리 가능해요.");

    }
```

- 와일드카드의 세 종류 (간단 요약)
  - <?> (Unbounded Wildcard): 제한 없음. 모든 타입을 다 받음.
  - <? extends T> (Upper Bounded): T와 그 자식들만 가능. (주로 데이터를 꺼낼 때 안전함)
  - <? super T> (Lower Bounded): T와 그 부모들만 가능. (주로 데이터를 넣을 때 안전함)
