**오늘의 한 줄 평:** 

#### 꿀팁
- 종료 코드 0(으)로 완료된 프로세스 : 한 줄 요약: "완벽하게 잘 끝났다!"
- 컴퓨터 세계에서 숫자 '0'은 '문제없음(Success)'을 의미해. 즉, 네가 짠 코드가 처음부터 끝까지 에러 하나 없이 의도한 대로 실행을 마치고 무사히 종료됐다는 뜻 이야.

2026-05-06 수업 21일차
#### 전 시간 복습
- 1. 오버로딩: 하나의 클래스 안에서 이름은 같지만 매개변수(파라미터)가 다른 메서드를 여러개 만드는 것.
- 2. 오버라이딩 : 상속 관계에서 부모클래스가 정의한 메서드를 자식 클래스에서 자기 입맛에 맞게 재정의 하는 것.
### Static 필드
- static 변수는 프로그램 시작 시 static 영역에 단 하나만 생성 된다. 공유 된다.
- 구조
```
heap   static  
a[]  --> [] (공유)
 []
```
- 변수 접근 차이 
   - 객체에 속한 일반 변수는 this 레퍼런스 변수로 접근
   - 객체에 속하지 않은 static 변수는 클래스명.필드명로 접근
```
    private int personalLocker; //non-static field : 개인 사물함

    private static int sharedLocker; //sharedLocker : 공용 사물함

    public int getPersonalLocker(){
        return this.personalLocker; //객체에 속한 일반 변수는 this 레퍼런스 변수로 접근
    }
    public int getSharedLocker(){
        return UniversityStudent.sharedLocker; //객체에 속하지 않은 static 변수는 클래스명.필드명로 접근
    }
```
- static 메소드안에서 객체 없이 존재하는 메소드이기 때문에 나 자신이 존재하지 않으므로 this 키워드 사용할 수 없다.
```
    public static void libararyAnnouncement(){
        this.personalLocker++;
    }
```
### final 키워드
- final을 붙이면 값을 바꿀 수 없음

### 싱글톤 패턴
- 1. 클래스가 로딩되는 즉시 인스턴스를 딱 하나 만든다.
- 2. 클래스 생성자에 private 키워드
- 3. getInstance() 메서드에 생성자 초기화

### 변수의 종류
- 1. 인스턴스 변수(non-static field)
  - 클래스 영역에 선언
  - new 키워드로 객체 생성과 함께 heap 영역에 생성.
  - 같은 클래스 내 어느 메서드에서든 접근 가능
  - 객체가 살아있을 때 까지만 변수도 산다.
- 2. 클래스 변수(static field)
  - 프로그램 시작과 함께 static 영역에 단 하나 생성
  - 모든 인스턴스가 공유
  - 프로그램이 종료 될 때까지 static 영역에서 사라지지 않는다.
  - 객체의 살아있음과 관계 없이 존재한다.
- 3. 지역 변수(local variable)
  - 메서드 블록 안에 선언. 메소드가 끝나면 사라진다.
정리 표
인스턴스 변수 | 클래스 영역 | Heap | new 실행시
클래스 변수 | 클래스 영역 + static | Static | 프로그램 시작
지역 변수 | 메서드/블록 내부 | Stack | 메서드 호출

- 초기화 개념 정말 중요하니 꼭 이해하기!
- Product.java
```
package com.ohgiraffers.section07.initblock;

public class Product {

    //명시적 초기화: 필드를 선언하면서 값 할당
    //객체가 생성될 때 가장 먼저 이 값으로 초기화 된다.
    private String name = "갤럭시";
    private int price = 15000000;
    private static String brand = "삼송";
    //static 초기화 블록 -> 명시적 초기값 -> 인스턴스 초기화 블록 -> 생성자

    //인스턴스 초기화 블럭: 생성자 보다 먼저, 모든 생성자 공통 실행
    //오버로딩된 생성자가 여러 개 일 때 중복 초기화 코드를 한 곳에 모으는 용도
    {
        name = "아이폰";
        price = 1800000;
        brand = "사과";
        System.out.println("인스턴스 초기화 블럭 {} 동작함..");
    }
    //정적 초기화 블럭: 클래스가 메모리에 로드될 때 딱 한 번 실행
    //복잡한 로직(반복문, DB 조회 등)으로 static 필드를 초기화 할 때 사용
    static {
        //name = "아이뽄";
        //new를 이용해 객체를 만든 상태가 아니기 때문에,
        //아직 인스턴스가 없어 인스턴스 변수에는 접근 불가
        //brand = "헬지";
        System.out.println("정적 초기화 블럭 static{} 동작함...");
    }

    //기본 생성자
    public Product(){ //기본 생성자는 적지 않아도 자바 컴파일러가 생성해줌
        System.out.println("기본 생성자 public Product() 호출됨...");
    }

    //매개변수 있는 생성자
    public Product(String name, int price, String brand)
    {
        this.name = name;
        this.price = price;
        Product.brand = brand;
        System.out.println("매개변수 있는 생성자 public Product(String name, int price, String brand) 호출됨...");
    }

    public String getInformation(){
        return this.name + ", " + this.price + ", " + Product.brand;
    }

}
```
Application.java
```
public class Application {
    public static void main(String[] args) {
        /*[객체 탄생 순서]
        * 1. static 초기화 블록  -> 클래스 로드 시 딱 1번
        * 2. 명시적 초기값       -> new 할 때 마다
        * 3. 인스턴스 초기화 블록 -> 생성자 직전 마다
        * 4. 생성자             -> 최종 마무리
        * */
        System.out.println("==product 생성==");
        Product product = new Product();

        System.out.println("==product2 생성==");
        Product product2 = new Product("대륙폰", 50000, "샤우미");

        System.out.println("===결과 출력==");
        System.out.println(product.getInformation());
        System.out.println(product2.getInformation());
    }
}
```
결과
```
==product 생성==
정적 초기화 블럭 static{} 동작함...
인스턴스 초기화 블럭 {} 동작함..
기본 생성자 public Product() 호출됨...
==product2 생성==
인스턴스 초기화 블럭 {} 동작함..
매개변수 있는 생성자 public Product(String name, int price, String brand) 호출됨...
===결과 출력==
아이폰, 1800000, 샤우미
대륙폰, 50000, 샤우미
```

### Java 접근 제어자
- 접근 제어자를 표기할 때는 +, -, #, ~, 밑줄, / 로 표시할 수 있다.
- (접근) 제어자	표기
- public	+
- private	-
- protected	#
- default(package)	~
- static	밑줄